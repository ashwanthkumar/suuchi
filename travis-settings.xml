<!--
    travis-settings.xml

We used this to publish our artifacts to sonatype snapshots after every build!
 -->
<servers>
    <server>
        <!-- Maven Central Deployment -->
        <id>ossrh</id>
        <username>${env.SONATYPE_USERNAME}</username>
        <password>${env.SONATYPE_PASSWORD}</password>
    </server>
</servers>
