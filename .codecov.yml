comment:
  layout: "header, diff, changes, sunburst"
  behavior: default
  require_changes: false  # if true: only post the comment if coverage changes
  branches: null
  flags: null
  paths: null

ignore:
  - suuchi-examples